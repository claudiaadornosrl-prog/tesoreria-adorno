# Credenciales y secretos — NUNCA subir
scrapers/.env

# Perfil de Chrome del scraper (cookies de sesión del banco)
scrapers/_state_galicia/
scrapers/_state_*/

# Python
scrapers/__pycache__/
*.pyc

# Logs y archivos temporales del scraper
scrapers/*.log
scrapers/*.csv

# ── REPO PÚBLICO MÍNIMO (2026-08-06) ─────────────────────────────
# Solo se commitea lo que GitHub Pages sirve. Todo lo operativo
# (scrapers, SQL, edge functions) queda SOLO en disco local.
scrapers/
sql/
supabase/
